import { Styles } from "./components/types";

const styles: Styles = {
    componentsContainer: {
        width: "90%",
        maxWidth: 960,
        margin: "0 auto",
    },
};

export default styles;
