import { Styles } from "../types";

const styles: Styles = {
    container: {
        display: "flex",
        justifyContent: "center",
        background: "#e2e2e2",
        marginBottom: 20,
        borderRadius: 8,
    },
    item: {
        marginRight: 16,
        padding: 20,
        cursor: "pointer",
    },
    selected: {
        color: "#1DA1F2",
    },
};

export default styles;
