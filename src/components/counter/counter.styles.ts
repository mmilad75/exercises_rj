import { Styles } from "../types";

const styles: Styles = {
    container: {
        textAlign: "center",
    },
    button: {
        padding: "0.5rem 1rem",
        margin: "0 0.5rem",
        border: "none",
        borderRadius: 4,
        background: "#14171A",
        color: "#fff",
    },
    select: {
        padding: "0.4rem 1rem",
        margin: "0 0.5rem",
        borderColor: "#14171A",
        borderRadius: 4,
    },
};

export default styles;
